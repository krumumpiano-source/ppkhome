# ระบบสิทธิ์ผู้ใช้และเมนู (Role & Permission) สำหรับที่พักอาศัย

เอกสารนี้สรุปแนวทางออกแบบระบบสิทธิ์ผู้ใช้แบบยืดหยุ่นสำหรับ
บริบท “ที่พัก/บ้านพักครู/แฟลต/หอพัก” โดยยึดหลักว่าเมนูเป็นทรัพยากร
และแอดมินสามารถ override สิทธิ์รายเมนูรายคนได้

---

## 1) Executive Summary (สรุปใจความหลัก)
- ระบบต้องมี **Top Navigation Bar** ที่เป็นเมนูชุดเดียวกันทุกคน
  แต่ **แสดง/ซ่อนตามสิทธิ์** ผู้ใช้เท่านั้น
- ต้องรองรับ **Role มาตรฐานหลายประเภท** (แอดมิน / บันทึกน้ำ /
  บันทึกไฟ / บัญชี / ผู้พัก / คนนอก) และให้เป็น **ค่าเริ่มต้น**
- **แอดมินสามารถ override สิทธิ์รายเมนูรายคน** ได้อย่างอิสระ
- โครงสร้างสิทธิ์ต้องเป็น **Menu-based** (ไม่ผูกกับหน้าเพจหรือ role)
- ใช้งานจริงในบริบทที่พักอาศัย และต้องต่อยอดได้ในอนาคต (เว็บ/มือถือ)

---

## 2) Action Steps (แตกเป็นขั้นตอนปฏิบัติ)

### Step 1: กำหนดเมนูระบบ (Menu Registry)
สร้าง “เมนูกลาง” 1 ชุดเป็น **Master ของระบบ** (ไม่ผูกกับ role ใด)

**หลักการ:**
- ทุกเมนูต้องมี **Menu ID** ที่ชัดเจนและคงที่
- ใช้ Menu ID เดียวกันทั้ง Frontend และ Backend
- เมนูทั้งหมดคือแหล่งอ้างอิงหลักสำหรับสิทธิ์

**Menu Registry (Top Bar):**
| Menu ID | เมนู | หมายเหตุ |
|---|---|---|
| MENU_BILLING_DUE | ยอดต้องชำระ | แสดงยอดค้างชำระ |
| MENU_BILLING_STATUS | สถานะการชำระ | ตรวจสอบสถานะ |
| MENU_BILLING_SUBMIT | ส่งสลิป | อัปโหลดหลักฐาน |
| MENU_WATER_ENTRY | บันทึกค่าน้ำ | ลงมิเตอร์/ค่าน้ำ |
| MENU_ELECTRIC_ENTRY | บันทึกค่าไฟ | ลงมิเตอร์/ค่าไฟ |
| MENU_MONTHLY_DISBURSE | เบิกจ่ายประจำเดือน | จัดการการเบิกจ่าย |
| MENU_ACCOUNTING_LEDGER | บัญชีรายรับรายจ่าย | ภาพรวมการเงิน |
| MENU_SLIP_REVIEW | ตรวจสลิป | ตรวจสอบ/อนุมัติ |
| MENU_PROFILE_SETTINGS | ตั้งค่าข้อมูลส่วนตัว | โปรไฟล์ผู้ใช้ |
| MENU_ADMIN_SETTINGS | ตั้งค่าแอดมิน | จัดการระบบ |
| MENU_RULES_FORMS | ระเบียบ / แบบฟอร์ม | ซ่อม/ย้าย/คืน |
| MENU_MANUAL | คู่มือ | คู่มือการใช้งาน |
| MENU_REPORTS_STATS | สถิติและการรายงานผล | รายงาน/สถิติ |

> หมายเหตุ: ถ้ามีเมนูย่อยภายหลัง ให้เพิ่มใน Registry และคง Menu ID เดิมเสมอ

---

### Step 2: กำหนด Role มาตรฐาน (Default Roles)
สร้าง role preset เป็นค่าเริ่มต้นเท่านั้น (ไม่ผูกตายตัว):
- ADMIN
- WATER_STAFF
- ELECTRIC_STAFF
- ACCOUNTING
- RESIDENT
- EXTERNAL

> Role เหล่านี้มีไว้กำหนด default permission เท่านั้น

---

### Step 3: ออกแบบ Permission Model
ใช้โครงสร้างหลัก **User / Role / Menu / Permission** เท่านั้น

**หลักการสำคัญ:**
- User ≠ Role (ผู้ใช้ไม่เท่ากับบทบาท)
- ผู้ใช้ 1 คนมี **หลายสิทธิ์** ได้
- แอดมินสามารถ “ติ๊กเปิด–ปิดเมนู” ให้ใครก็ได้ (รายเมนู รายคน)

**Data Model (ตัวอย่างตาราง/อ็อบเจ็กต์):**
- `users` (id, email, default_role_id, status, ...)
- `roles` (id, code, name)
- `menus` (id, code, label, path, order)
- `permissions` (id, code: VIEW/EDIT/APPROVE, ...)
- `role_permissions` (role_id, menu_id, permission_id, allow)
- `user_permissions` (user_id, menu_id, permission_id, allow) ← override รายคน
- `audit_logs` (actor_user_id, target_user_id, action, before, after, created_at)

**Effective Permission Logic (สรุป):**
1. โหลดสิทธิ์จาก **role_permissions**
2. ซ้อนทับด้วย **user_permissions** (override รายคน)
3. ถ้ามีทั้ง allow และ deny → **deny ชนะ**
4. สร้าง `allowedMenus[]` สำหรับ Frontend

---

### Step 4: Logic การแสดงเมนู
**Frontend ต้องทำ:**
- หลัง login โหลด `allowedMenus[]` (array ของ Menu ID)
- Render Top Bar ตาม array นี้เท่านั้น
- ❌ ห้าม hardcode เมนูตาม role

**Backend ต้องทำ:**
- ตรวจสิทธิ์ซ้ำทุก API (กันเปิด URL เอง)
- ใช้ Menu ID เดียวกันกับ Registry ในการตรวจสิทธิ์

---

### Step 5: หน้าตั้งค่าแอดมิน (หัวใจระบบ)
ต้องมี:
- เลือกผู้ใช้ (Search / Dropdown)
- Checkbox รายเมนู (VIEW/EDIT/APPROVE ตามต้องการ)
- ปุ่ม **Reset** = กลับค่า default role
- **Audit log** (ใครแก้สิทธิ์ใคร เมื่อไหร่)

---

## 3) Action Plan (เอาไปทำงานต่อได้เลย)

| เป้าหมาย | งานย่อย | ผลลัพธ์ |
|---|---|---|
| โครงเมนู | สร้าง Menu Registry | เมนู ID กลาง |
| ระบบสิทธิ์ | Role + User Permission | ปรับสิทธิ์ได้อิสระ |
| UI | Top Bar แบบ Dynamic | เห็นเมนูตามสิทธิ์ |
| Admin Control | หน้าแก้สิทธิ์ | แอดมินคุมได้หมด |
| ความปลอดภัย | Backend permission check | กันหลุดจริง |

---

## 4) Prompt สำหรับ AI / Cursor (คัดลอกใช้ได้ทันที)

### 🔹 Prompt 1: ออกแบบโครงสร้างสิทธิ์
```text
You are a senior system architect.

Design a role & permission system for a web-based housing management system.

Requirements:
- Top navigation menu with multiple modules
- Default roles: Admin, Water Staff, Electric Staff, Accounting, Resident, External
- Admin can freely override menu access per user
- Menu-based permission (not page-based)
- Must support future expansion

Output:
- Data model (tables / objects)
- Permission logic explanation
- Best practices and pitfalls
```

### 🔹 Prompt 2: สั่ง Cursor เขียน Frontend Menu Logic
```text
You are a senior frontend engineer.

Build a dynamic top navigation bar.
Menus must be rendered only if allowed by user permissions.

Constraints:
- Menu list comes from backend as array of menu IDs
- No hardcoded role checks
- Clean, maintainable structure

Output:
- Example JS/HTML structure
- Permission-based rendering logic
```

### 🔹 Prompt 3: สั่ง Cursor ทำ Admin Permission Page
```text
You are a full-stack engineer.

Create an admin page to manage user permissions.

Requirements:
- Select user
- Show all menus with checkboxes
- Save permissions per user
- Reset to role default
- Audit log for changes

Output:
- UI layout suggestion
- API endpoints
- Data flow
```

---

## 5) หมายเหตุเชิงคนทำระบบ (พูดตรง)
- ❌ อย่าผูก “เมนู = role” เด็ดขาด → ระบบจะตัน
- ✅ คิดแบบ “เมนูเป็นทรัพยากร”
- ✅ Admin override = ระบบโตได้จริง
- ✅ ถ้าจะต่อมือถือ/แอป โครงนี้ยังใช้ได้
