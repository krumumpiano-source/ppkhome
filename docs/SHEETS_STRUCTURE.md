# โครงสร้าง Google Sheets - ระบบบริหารจัดการบ้านพักครู

## 📋 สารบัญ

1. [โครงสร้างโฟลเดอร์](#โครงสร้างโฟลเดอร์)
2. [Spreadsheet หลัก](#spreadsheet-หลัก)
3. [รายละเอียด Sheets และ Columns](#รายละเอียด-sheets-และ-columns)
4. [การตั้งค่า Script Properties](#การตั้งค่า-script-properties)

---

## 📁 โครงสร้างโฟลเดอร์

### Google Drive Structure
```
📁 Teacher Housing Management System/
  └── 📊 Main Spreadsheet (SPREADSHEET_ID)
      ├── Sheet: Users
      ├── Sheet: Units
      ├── Sheet: SysConfig
      ├── Sheet: WaterReadings
      ├── Sheet: ElectricReadings
      ├── Sheet: BillingRounds
      ├── Sheet: Payments
      ├── Sheet: CentralLedger
      ├── Sheet: Applications
      ├── Sheet: Queue
      ├── Sheet: AboutContent
      ├── Sheet: AuditLog
      ├── Sheet: RegistrationRequests
      └── Sheet: RepairRequests
```

**หมายเหตุ**: ระบบใช้ Spreadsheet เดียวที่มีหลาย Sheets (Tabs) ไม่ใช่หลายไฟล์แยกกัน

---

## 📊 Spreadsheet หลัก

### ข้อมูลพื้นฐาน
- **ชื่อ**: Teacher Housing Management System
- **Spreadsheet ID**: `1X_I7Yj1xm8IOszkD37MvVG8WUGbBrxdK6Os9nyc3UmY`
- **URL**: https://docs.google.com/spreadsheets/d/1X_I7Yj1xm8IOszkD37MvVG8WUGbBrxdK6Os9nyc3UmY/edit
- **ประเภท**: Google Spreadsheet

### การตั้งค่า SPREADSHEET_ID (Optional)
ระบบจะใช้ค่า default ที่ตั้งไว้แล้ว แต่ถ้าต้องการเปลี่ยน:
1. เปิด Google Apps Script Editor
2. ไปที่ **Project Settings** → **Script Properties**
3. เพิ่ม Property:
   - **Key**: `SPREADSHEET_ID`
   - **Value**: ID ของ Spreadsheet (จาก URL)

---

## 📁 Google Drive Folders

### โฟลเดอร์สำหรับเก็บไฟล์

| โฟลเดอร์ | Folder ID | วัตถุประสงค์ |
|---------|-----------|-------------|
| **PaymentSlips** | `1RK8PFdrFFGw_6gsmUxDrUqtCWZYuhUJF` | เก็บสลิปการชำระเงิน |
| **AboutImages** | `1wVbFJ90GOoxM0FHe8ks8ddpRdw3OCrwQ` | เก็บรูปภาพหน้าเกี่ยวกับ |
| **Exports** | `1JCt9ooyxkGvvKyhG-HuEysqf0ghKbgsg` | เก็บไฟล์ Export ข้อมูล |

### การตั้งค่า Folder IDs (Optional)
ระบบจะใช้ค่า default ที่ตั้งไว้แล้ว แต่ถ้าต้องการเปลี่ยน:
1. เปิด Google Apps Script Editor
2. ไปที่ **Project Settings** → **Script Properties**
3. เพิ่ม Properties:
   - **Key**: `DRIVE_FOLDER_PAYMENT_SLIPS` → **Value**: Folder ID
   - **Key**: `DRIVE_FOLDER_ABOUT_IMAGES` → **Value**: Folder ID
   - **Key**: `DRIVE_FOLDER_EXPORTS` → **Value**: Folder ID

---

## 📑 รายละเอียด Sheets และ Columns

### 1. Sheet: `Users`
**วัตถุประสงค์**: เก็บข้อมูลผู้ใช้ทั้งหมด

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `userId` | Text | Primary Key (เช่น `user_1234567890`) |
| B | `email` | Text | อีเมล (Unique, ใช้สำหรับ Login) |
| C | `passwordHash` | Text | รหัสผ่าน (Hash) - ไม่เก็บ plain text |
| D | `fullName` | Text | ชื่อ-นามสกุล |
| E | `phone` | Text | เบอร์โทรศัพท์ |
| F | `role` | Text | บทบาท: `resident`, `committee`, `accounting`, `admin`, `deputy_admin`, `executive`, `applicant` |
| G | `unitId` | Text | รหัสหน่วย (เช่น `1`, `17`, `F1`, `F16`) หรือว่าง |
| H | `status` | Text | สถานะ: `active`, `inactive`, `pending` |
| I | `householdMembers` | Text | สมาชิกในบ้าน (JSON หรือ Text) |
| J | `mustChangePassword` | Boolean | ต้องเปลี่ยนรหัสผ่าน (`true`/`false`) |
| K | `createdAt` | Date | วันที่สร้าง |
| L | `updatedAt` | Date | วันที่อัปเดตล่าสุด |
| M | `birthDate` | Date | วันเดือนปีเกิด |
| N | `subjectGroup` | Text | กลุ่มสาระการเรียนรู้ที่ทำงานอยู่ |
| O | `houseRegNo` | Text | เลขทะเบียนบ้าน |
| P | `houseNo` | Text | บ้านเลขที่ |
| Q | `villageName` | Text | หมู่บ้าน/อาคาร |
| R | `moo` | Text | หมู่ |
| S | `soi` | Text | ซอย |
| T | `road` | Text | ถนน |
| U | `subdistrict` | Text | ตำบล/แขวง |
| V | `district` | Text | อำเภอ/เขต |
| W | `province` | Text | จังหวัด |
| X | `postalCode` | Text | รหัสไปรษณีย์ |
| Y | `pdpaConsent` | Boolean | ยินยอม PDPA |
| Z | `pdpaConsentAt` | Date | วันที่ให้ความยินยอม |

**ตัวอย่าง Header Row**:
```
userId | email | passwordHash | fullName | phone | role | unitId | status | householdMembers | mustChangePassword | createdAt | updatedAt | birthDate | subjectGroup | houseRegNo | houseNo | villageName | moo | soi | road | subdistrict | district | province | postalCode | pdpaConsent | pdpaConsentAt
```

---

### 2. Sheet: `Units`
**วัตถุประสงค์**: ข้อมูลบ้านพักและแฟลต

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `unitId` | Text | Primary Key (`1`-`17` หรือ `F1`-`F16`) |
| B | `type` | Text | ประเภท: `house` หรือ `flat` |
| C | `status` | Text | สถานะ: `occupied`, `vacant` |
| D | `notes` | Text | หมายเหตุเพิ่มเติม |

**ตัวอย่าง Header Row**:
```
unitId | type | status | notes
```

**ข้อมูลเริ่มต้น**:
- บ้านพัก: `1` ถึง `17` (type: `house`)
- แฟลต: `F1` ถึง `F16` (type: `flat`)

---

### 3. Sheet: `SysConfig`
**วัตถุประสงค์**: ตั้งค่าระบบ (Key-Value)

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `key` | Text | ชื่อคีย์ (Unique) |
| B | `value` | Text/Number | ค่าที่ตั้ง |

**ตัวอย่าง Header Row**:
```
key | value
```

**Keys ที่ใช้**:
- `waterRate`: ราคาน้ำต่อหน่วย (Number, default: 9)
- `commonFee`: ค่าส่วนกลาง (Number, default: 0)
- `vacantMeterFee`: ค่าอ่านมิเตอร์บ้านว่าง (Number, default: 9)
- `holidayDates`: วันหยุด (Text, format: `yyyy-MM-dd,yyyy-MM-dd,...`)
- `queueEndDate`: วันสิ้นสุดคิว (Date)
- `approvalEnabled`: เปิด/ปิดการอนุมัติ (Boolean: `true`/`false`)

---

### 4. Sheet: `WaterReadings`
**วัตถุประสงค์**: บันทึกมิเตอร์น้ำ

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `roundId` | Text | รอบการเรียกเก็บ (เช่น `2024-01`) |
| B | `unitId` | Text | รหัสหน่วย (`1`-`17` หรือ `F1`-`F16`) |
| C | `prevReading` | Number | เลขมิเตอร์ก่อนหน้า |
| D | `prevDate` | Date | วันที่อ่านครั้งก่อน |
| E | `currentReading` | Number | เลขมิเตอร์ปัจจุบัน |
| F | `amount` | Number | จำนวนเงิน (หน่วย × ราคา) |
| G | `note` | Text | หมายเหตุ |

**ตัวอย่าง Header Row**:
```
roundId | unitId | prevReading | prevDate | currentReading | amount | note
```

---

### 5. Sheet: `ElectricReadings`
**วัตถุประสงค์**: บันทึกค่าไฟ

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `roundId` | Text | รอบการเรียกเก็บ |
| B | `unitId` | Text | รหัสหน่วย |
| C | `amount` | Number | จำนวนเงินค่าไฟ (ปัดขึ้น) |
| D | `date` | Date | วันที่บันทึก |
| E | `total` | Number | ยอดรวม (ซ้ำกับ amount) |
| F | `userId` | Text | ผู้บันทึก |

**ตัวอย่าง Header Row**:
```
roundId | unitId | amount | date | total | userId
```

**หมายเหตุ**: ระบบยังรองรับการกรอก `totalBill`, `lostHouse`, `lostFlat` ในรอบเดียวกัน

---

### 6. Sheet: `BillingRounds`
**วัตถุประสงค์**: รอบการเรียกเก็บค่าใช้จ่าย

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `roundId` | Text | Primary Key (เช่น `2024-01`) |
| B | `month` | Number | เดือน (1-12) |
| C | `year` | Number | ปี (เช่น 2024) |
| D | `status` | Text | สถานะ: `open`, `closed` |
| E | `billingDate` | Date | วันที่แจ้งยอด (ถ้าไม่ระบุใช้วันที่ 1 ของเดือน) |

**ตัวอย่าง Header Row**:
```
roundId | month | year | status | billingDate
```

---

### 7. Sheet: `Payments`
**วัตถุประสงค์**: บันทึกการชำระเงิน

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `roundId` | Text | รอบการเรียกเก็บ |
| B | `unitId` | Text | รหัสหน่วย |
| C | `amount` | Number | จำนวนเงินที่ชำระ |
| D | `date` | Date | วันที่ชำระ |
| E | `userId` | Text | ผู้ชำระเงิน |
| F | `verified` | Boolean | ตรวจสอบแล้วหรือยัง (`true`/`false`) |
| G | `note` | Text | หมายเหตุ |
| H | `slipDataUrl` | Text | URL ของสลิป (Base64 หรือ Drive URL) |

**ตัวอย่าง Header Row**:
```
roundId | unitId | amount | date | userId | verified | note | slipDataUrl
```

---

### 8. Sheet: `CentralLedger`
**วัตถุประสงค์**: บัญชีรายรับ-รายจ่ายกองกลาง

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `roundId` | Text | รอบการเรียกเก็บ |
| B | `date` | Date | วันที่รายการ |
| C | `type` | Text | ประเภท: `income` หรือ `expense` |
| D | `description` | Text | รายละเอียด |
| E | `amount` | Number | จำนวนเงิน |

**ตัวอย่าง Header Row**:
```
roundId | date | type | description | amount
```

---

### 9. Sheet: `Applications`
**วัตถุประสงค์**: คำร้องขอเข้าพัก

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `applicationId` | Text | Primary Key (เช่น `app_1234567890`) |
| B | `fullName` | Text | ชื่อ-นามสกุล |
| C | `email` | Text | อีเมล |
| D | `phone` | Text | เบอร์โทรศัพท์ |
| E | `reason` | Text | เหตุผลขอเข้าพัก |
| F | `status` | Text | สถานะ: `pending`, `approved`, `rejected` |
| G | `submittedAt` | Date | วันที่ยื่นคำร้อง |

**ตัวอย่าง Header Row**:
```
applicationId | fullName | email | phone | reason | status | submittedAt
```

---

### 10. Sheet: `Queue`
**วัตถุประสงค์**: คิวคำร้องขอเข้าพัก

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `applicationId` | Text | Foreign Key → Applications |
| B | `order` | Number | ลำดับในคิว (0-based) |
| C | `status` | Text | สถานะ: `pending`, `in_queue`, `processed`, `expired` |
| D | `createdAt` | Date | วันที่เข้าคิว |
| E | `expiry` | Date | วันหมดอายุคิว |

**ตัวอย่าง Header Row**:
```
applicationId | order | status | createdAt | expiry
```

---

### 11. Sheet: `AboutContent`
**วัตถุประสงค์**: เนื้อหาหน้าเกี่ยวกับบ้านพักครู

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `sectionId` | Text | Primary Key (เช่น `basic`, `policy`) |
| B | `title` | Text | หัวข้อ |
| C | `body` | Text | เนื้อหา |
| D | `imageUrl` | Text | URL รูปภาพ (ถ้ามี) |
| E | `visible` | Boolean | แสดงหรือไม่ (`true`/`false`) |
| F | `order` | Number | ลำดับการแสดง (น้อยกว่า = แสดงก่อน) |

**ตัวอย่าง Header Row**:
```
sectionId | title | body | imageUrl | visible | order
```

---

### 12. Sheet: `AuditLog`
**วัตถุประสงค์**: บันทึกการใช้งาน (Audit Trail)

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `timestamp` | Date | วันที่และเวลา |
| B | `userId` | Text | ผู้ดำเนินการ |
| C | `action` | Text | การกระทำ (เช่น `login`, `logout`, `billing_submit`) |
| D | `detail` | Text | รายละเอียด (JSON string) |

**ตัวอย่าง Header Row**:
```
timestamp | userId | action | detail
```

**Actions ที่ใช้**:
- `login`, `logout`
- `profile_update`
- `billing_submit`
- `water_reading`
- `electric_reading`
- `repair_request`
- `bank_verify`
- `data_import`, `data_export`
- และอื่นๆ ตามที่ระบบบันทึก

---

### 13. Sheet: `RegistrationRequests`
**วัตถุประสงค์**: คำขอลงทะเบียนใช้งานระบบ

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `requestId` | Text | Primary Key (เช่น `req_1234567890`) |
| B | `fullName` | Text | ชื่อ-นามสกุล |
| C | `email` | Text | อีเมล (Unique) |
| D | `phone` | Text | เบอร์โทรศัพท์ |
| E | `status` | Text | สถานะ: `pending`, `approved`, `rejected` |
| F | `requestedAt` | Date | วันที่ส่งคำขอ |

**ตัวอย่าง Header Row**:
```
requestId | fullName | email | phone | status | requestedAt
```

---

### 14. Sheet: `RepairRequests`
**วัตถุประสงค์**: คำร้องแจ้งซ่อม/ปรับปรุง/ต่อเติม

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `requestId` | Text | Primary Key (เช่น `rep_1234567890`) |
| B | `unitId` | Text | รหัสหน่วย |
| C | `type` | Text | ประเภท: `แจ้งซ่อม`, `ปรับปรุง`, `ต่อเติม` |
| D | `note` | Text | รายละเอียด |
| E | `status` | Text | สถานะ: `pending`, `in_progress`, `completed`, `cancelled` |
| F | `requestedAt` | Date | วันที่ส่งคำร้อง |

**ตัวอย่าง Header Row**:
```
requestId | unitId | type | note | status | requestedAt
```

---

### 15. Sheet: `ResidentRequests`
**วัตถุประสงค์**: คำร้องขอย้ายห้อง/ย้ายบ้าน และคำร้องขอคืนบ้านพัก

| Column | ชื่อคอลัมน์ | ประเภทข้อมูล | หมายเหตุ |
|--------|------------|-------------|----------|
| A | `requestId` | Text | Primary Key (เช่น `req_1234567890`) |
| B | `userId` | Text | รหัสผู้ใช้งาน |
| C | `unitId` | Text | รหัสหน่วย |
| D | `requestType` | Text | ประเภท: `move`, `return` |
| E | `requestSubType` | Text | ย้ายห้อง/ย้ายบ้าน (optional) |
| F | `desiredUnitId` | Text | หน่วยที่ต้องการ (optional) |
| G | `desiredMoveDate` | Date | วันที่ต้องการย้าย (optional) |
| H | `moveOutDate` | Date | วันที่ขอคืนบ้านพัก (optional) |
| I | `reason` | Text | เหตุผล/รายละเอียด |
| J | `contactPhone` | Text | เบอร์ติดต่อ |
| K | `status` | Text | สถานะ: `pending`, `approved`, `rejected`, `in_progress` |
| L | `pdpaConsent` | Boolean | ยินยอม PDPA |
| M | `pdpaConsentAt` | Date | วันที่ให้ความยินยอม |
| N | `createdAt` | Date | วันที่ส่งคำร้อง |

**ตัวอย่าง Header Row**:
```
requestId | userId | unitId | requestType | requestSubType | desiredUnitId | desiredMoveDate | moveOutDate | reason | contactPhone | status | pdpaConsent | pdpaConsentAt | createdAt
```

---

## ⚙️ การตั้งค่า Script Properties

### SPREADSHEET_ID
**วิธีตั้งค่า**:
1. เปิด Google Apps Script Editor
2. ไปที่ **Project Settings** (⚙️) → **Script Properties**
3. คลิก **Add script property**
4. กรอก:
   - **Property**: `SPREADSHEET_ID`
   - **Value**: ID ของ Spreadsheet (จาก URL)

**ตัวอย่าง URL**:
```
https://docs.google.com/spreadsheets/d/1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms/edit
```
→ SPREADSHEET_ID = `1BxiMVs0XRA5nFMdKvBdBZjgmUUqptlbs74OgvE2upms`

---

## 📝 หมายเหตุสำคัญ

### 1. Header Row
- **ระบบจะสร้าง Header Row อัตโนมัติ** เมื่อ Sheet ว่างเปล่า
- ไม่ต้องสร้าง Header Row เอง
- ชื่อคอลัมน์ต้องตรงกับที่ระบุในเอกสารนี้
- ระบบจะอ่าน Header Row เพื่อหา Column Index

### 2. Data Types
- **Text**: เก็บเป็น String
- **Number**: เก็บเป็น Number (ไม่ใช่ Text)
- **Date**: เก็บเป็น Date object หรือ ISO string
- **Boolean**: เก็บเป็น `true`/`false` หรือ `TRUE`/`FALSE`

### 3. Primary Keys
- ใช้ Text format (เช่น `user_1234567890`, `app_1234567890`)
- ต้อง Unique ใน Sheet เดียวกัน

### 4. Foreign Keys
- `unitId` → อ้างอิง `Units.unitId`
- `applicationId` → อ้างอิง `Applications.applicationId`
- `roundId` → อ้างอิง `BillingRounds.roundId`
- `userId` → อ้างอิง `Users.userId`

### 5. การสร้าง Sheet ใหม่
เมื่อสร้าง Sheet ใหม่:
1. สร้าง Sheet ใน Spreadsheet หลัก
2. ตั้งชื่อ Sheet ให้ตรงกับ `CONFIG.SHEETS` ใน `config.gs`
3. **ไม่ต้องสร้าง Header Row** - ระบบจะสร้างอัตโนมัติเมื่อใช้งานครั้งแรก
4. ระบบจะใช้งานได้ทันที

---

## 🔄 การ Export/Import ข้อมูล

ระบบรองรับการ Export/Import ข้อมูลทั้งหมด:
- **Export**: สร้าง Spreadsheet ใหม่พร้อมข้อมูลทั้งหมด
- **Import**: นำเข้าข้อมูลจาก Spreadsheet ที่ส่งออก
- **Validation**: ตรวจสอบโครงสร้างก่อนนำเข้า
- **Transaction**: นำเข้าทั้งหมดหรือไม่นำเข้าเลย (All or Nothing)

ดูรายละเอียดเพิ่มเติมใน `docs/STANDARDIZATION.md`

---

## ✅ Checklist สำหรับการ Setup

- [x] สร้าง Google Spreadsheet ใหม่
- [x] ตั้งชื่อ Spreadsheet
- [x] สร้าง Sheets ทั้ง 14 แผ่นตามที่ระบุ
- [x] **Header Rows** - ระบบจะสร้างอัตโนมัติ (ไม่ต้องสร้างเอง)
- [x] ตั้งค่า SPREADSHEET_ID ใน Script Properties (หรือใช้ค่า default)
- [ ] เพิ่มข้อมูลเริ่มต้นใน `Units` (บ้าน 1-17, แฟลต F1-F16) - **Optional** (ใช้ฟังก์ชัน `setupInitialUnits()`)
- [ ] ตั้งค่า `SysConfig` (waterRate, commonFee, etc.) - **Optional** (ใช้ฟังก์ชัน `setupInitialSysConfig()`)
- [ ] สร้างผู้ใช้ Admin คนแรกใน `Users` - **Required** (ใช้ฟังก์ชัน `createFirstAdmin()`)
- [ ] ตั้งค่า Permissions ใน Google Apps Script - **Required**
- [ ] ทดสอบการเข้าถึงจากระบบ

---

**เอกสารนี้เป็นส่วนหนึ่งของระบบบริหารจัดการบ้านพักครู**  
**อัปเดตล่าสุด**: 2024  
**เวอร์ชัน**: 1.0
